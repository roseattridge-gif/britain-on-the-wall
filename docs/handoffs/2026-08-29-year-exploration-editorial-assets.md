# Year exploration and editorial asset infrastructure

Date: 2026-08-29

## Product outcome

The national Wall is now directly interrogable across all five accepted fiscal years. Direct selection, previous/next and keyboard controls all update the same spatial composition. Immigration is the sole topic proof and distinguishes government context, policy ownership, actual measurement period, inherited direction and after-handover chronology.

## Illustration correction

The generated source/destination PNG scene sheets and their CSS crop-coordinate system were removed. `src/assets/editorial/manifest.ts` now maps each stable fiscal taxonomy ID to a swappable editorial asset with alt text, category and focal point. Four restrained line-art placeholders occupy invariant source/destination boxes until bespoke commissioned artwork is supplied.

## Comparison rules

Every fiscal row contains selected nominal cash and share. It additionally reports nominal and percentage-point changes versus the previous accepted period and versus 2021–22. Positions do not reorder, preserving spatial memory. Net migration has `neutral-context` polarity, so the interface reports UP, PEAKED or DOWN and never recasts direction as political success or failure.

## Known boundary

Only authoritative net-migration points are shown for Immigration. No unsupported asylum-processing, hotels, returns or capacity series has been invented. The abstract placeholders are production-ready infrastructure, not claimed final commissioned artwork.

## Verification record

Six Vitest files / 110 tests passed. TypeScript and the Vite production build passed, as did `git diff --check`. All five states were rendered and inspected at 1440×900; paths are recorded in `docs/CODEX_HANDOFF.md`. Implementation SHA: `b15320b`.
