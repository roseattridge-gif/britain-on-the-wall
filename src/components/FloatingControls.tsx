import {Droplets,Layers,Minus,Plus,Scan} from 'lucide-react'; import type {Year} from '../types';
export function FloatingControls({year,setYear,unit,setUnit,leaks,setLeaks,receipt,setReceipt,zoomIn,zoomOut,fit}:{year:Year;setYear:(x:Year)=>void;unit:'pound'|'bn';setUnit:(x:'pound'|'bn')=>void;leaks:boolean;setLeaks:(x:boolean)=>void;receipt:boolean;setReceipt:(x:boolean)=>void;zoomIn:()=>void;zoomOut:()=>void;fit:()=>void}){return <><div className="canvas-tools" aria-label="Wall layers"><div className="compact-years">{([2005,2010,2015,2020,2025] as Year[]).map(y=><button key={y} className={year===y?'active':''} onClick={()=>setYear(y)}>{y===2025?'NOW':String(y).slice(2)}</button>)}</div><div className="compact-units"><button className={unit==='pound'?'active':''} onClick={()=>setUnit('pound')}>£1</button><button className={unit==='bn'?'active':''} onClick={()=>setUnit('bn')}>£bn</button></div><button aria-pressed={leaks} className={leaks?'active warning':''} onClick={()=>setLeaks(!leaks)}><Droplets/>Leaks</button><button aria-pressed={receipt} className={receipt?'active':''} onClick={()=>setReceipt(!receipt)}><Layers/>Contribution</button></div><div className="zoom-tools"><button onClick={zoomIn} aria-label="Zoom in"><Plus/></button><button onClick={zoomOut} aria-label="Zoom out"><Minus/></button><button onClick={fit} aria-label="Fit Britain"><Scan/><span>Fit Britain</span></button></div></>}
