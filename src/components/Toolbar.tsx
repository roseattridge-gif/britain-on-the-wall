import {Layers,GitCompareArrows,Droplets,Scale} from 'lucide-react'; import type {Year} from '../types';
export function Toolbar({year,setYear,unit,setUnit,leaks,setLeaks,receipt,setReceipt}:{year:Year;setYear:(x:Year)=>void;unit:'pound'|'bn';setUnit:(x:'pound'|'bn')=>void;leaks:boolean;setLeaks:(x:boolean)=>void;receipt:boolean;setReceipt:(x:boolean)=>void}){const years:Year[]=[2005,2010,2015,2020,2025];return <div className="toolbar" aria-label="Wall controls"><div className="year-control"><span>Move through time</span><div>{years.map(y=><button key={y} className={y===year?'active':''} onClick={()=>setYear(y)}>{y===2025?'Latest':y}</button>)}</div></div><div className="tool-pills"><div className="segmented"><button className={unit==='pound'?'active':''} onClick={()=>setUnit('pound')}>Every £1</button><button className={unit==='bn'?'active':''} onClick={()=>setUnit('bn')}>£ billions</button></div><button className="mode muted" title="Designed placeholder"><Scale/>Annual flow <span>Balance sheet soon</span></button><button className={receipt?'mode active':'mode'} aria-pressed={receipt} onClick={()=>setReceipt(!receipt)}><Layers/>Contribution & receipt</button><button className={leaks?'mode leak-on':'mode'} aria-pressed={leaks} onClick={()=>setLeaks(!leaks)}><Droplets/>Show leaks</button><button className="mode muted" title="Designed placeholder"><GitCompareArrows/>Compare <span>Soon</span></button></div></div>}
