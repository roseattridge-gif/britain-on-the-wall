import {X,ShieldCheck,ExternalLink} from 'lucide-react'; import type {Evidence} from '../types';
export function EvidenceDrawer({item,onClose}:{item?:Evidence;onClose:()=>void}){const status=item?.dataStatus??'illustrative';return <><button className={`drawer-scrim ${item?'open':''}`} onClick={onClose} tabIndex={-1} aria-hidden={!item}/><aside className={`drawer ${item?'open':''}`} aria-hidden={!item} aria-label="Evidence details" aria-live="polite"><div className="drawer-head"><div><span className="eyebrow">03 · INTERROGATE THE EVIDENCE</span><h2>{item?.metric??'Evidence'}</h2></div><button className="icon-button" onClick={onClose} aria-label="Close evidence"><X/></button></div>{item&&<><div className="evidence-value">{item.value===undefined?'Official evidence':`£${item.value.toFixed(3)}bn`}<span>{item.period} · {item.geography}</span></div><div className="confidence"><ShieldCheck/><span><b>{item.confidence} confidence</b>{status.replaceAll('-',' ')}</span></div><section className="evidence-section"><h3>Source</h3><p>{item.source}</p>{item.dataset&&<small>{item.dataset}</small>}</section><section className="evidence-section"><h3>Definition</h3><p>{item.definition}</p></section><section className="evidence-section"><h3>What this tells us</h3><p>{item.basis}</p></section><section className="evidence-section limitation"><h3>What this cannot tell us</h3><p>{item.limitations}</p></section><details><summary>Technical details</summary><dl>{Object.entries({Unit:item.unit,'Publication date':item.published,'Last checked':item.checked,Methodology:item.methodology,'Revision status':item.revision}).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></details>{item.url&&<a className="inspect-evidence" href={item.url} target="_blank" rel="noreferrer">Open official source <ExternalLink/></a>}</>}</aside></>}
