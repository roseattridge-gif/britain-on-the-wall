import {render,screen} from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import {describe,expect,it} from 'vitest'; import App from './App';
describe('semantic wall',()=>{
  it('keeps Britain and Health in one persistent canvas',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Health £23/}));expect(screen.getByRole('button',{name:'Britain'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'Health'})).toBeInTheDocument();expect(screen.getByRole('button',{name:/Medical services/})).toBeInTheDocument()});
  it('flies from Health into the medical-services operational lens',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Health £23/}));await userEvent.click(screen.getByRole('button',{name:/Medical services/}));expect(screen.getByRole('button',{name:/GP appointments/})).toBeInTheDocument();expect(screen.getByText(/Medical services is the largest UK/)).toBeInTheDocument()});
  it('steps back through semantic camera levels with Escape',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Health £23/}));await userEvent.click(screen.getByRole('button',{name:/Medical services/}));await userEvent.keyboard('{Escape}');expect(screen.getByRole('button',{name:/Medical services/})).toBeInTheDocument();expect(screen.queryByRole('button',{name:/GP appointments/})).not.toBeInTheDocument();await userEvent.keyboard('{Escape}');expect(screen.queryByRole('button',{name:'Health'})).not.toBeInTheDocument()});
  it('opens evidence without removing the wall',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Income & social contributions/}));await userEvent.click(screen.getByRole('button',{name:/Inspect evidence/}));expect(screen.getByText('Challenge the number')).toBeInTheDocument();expect(screen.getByLabelText(/Pannable and zoomable/)).toBeInTheDocument()});
  it('adds contribution layer to the same world',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Contribution/}));expect(screen.getByText('CONTRIBUTION & RECEIPT')).toBeInTheDocument()});
  it('teaches the national grammar without blocking the wall',async()=>{render(<App/>);expect(screen.getByText(/Start here: who funds Britain/)).toBeInTheDocument();expect(screen.getByLabelText(/Pannable and zoomable/)).toBeInTheDocument();await userEvent.click(screen.getByRole('button',{name:'Next'}));expect(screen.getByText(/Borrowing enters differently/)).toBeInTheDocument()});
  it('offers only the five accepted real outturn periods',()=>{render(<App/>);expect(screen.getByRole('slider',{name:'Fiscal period timeline'})).toBeInTheDocument();['2021–22','2022–23','2023–24','2024–25','2025–26'].forEach(period=>expect(screen.getByRole('button',{name:`Select fiscal period ${period}`})).toBeInTheDocument());expect(screen.queryByRole('button',{name:/2005/})).not.toBeInTheDocument()});
  it('changes the real Wall in place without resetting the selected territory',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Health £23/}));await userEvent.click(screen.getByRole('button',{name:'Select fiscal period 2021–22'}));expect(screen.getByRole('button',{name:/Health £25/})).toBeInTheDocument();expect(screen.getByRole('button',{name:'Health'})).toBeInTheDocument();expect(screen.getAllByText(/2021–22/).length).toBeGreaterThan(0)});
  it('shows current-price warning in billions mode',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:'£ BILLIONS'}));expect(screen.getByText(/CURRENT PRICES · NOT INFLATION ADJUSTED/)).toBeInTheDocument()});
  it('keeps illustrative attention stories out of real history',()=>{render(<App/>);expect(screen.queryByText('DEMO ATTENTION')).not.toBeInTheDocument();expect(screen.queryByText('RELATED SPEND')).not.toBeInTheDocument()});
  it('keeps outcomes explicitly non-causal and illustrative',()=>{render(<App/>);expect(screen.queryByText('RELATED SPEND')).not.toBeInTheDocument();expect(screen.getByText(/ILLUSTRATIVE CONTEXT · NOT HISTORICAL/)).toBeInTheDocument()});
  it('explains the real Health allocation to output and outcome chain',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Health £23/}));expect(screen.getByText(/Britain allocated £23 of every £100 here/)).toBeInTheDocument();expect(screen.getByText('PESA functional spend')).toBeInTheDocument();expect(screen.getByText(/383.4m GP appointments/)).toBeInTheDocument();expect(screen.getByText(/England operational lens/)).toBeInTheDocument()});
  it('encodes national magnitude as area, ribbon and editorial hierarchy',()=>{render(<App/>);const workers=screen.getByRole('button',{name:/Income & social contributions/});const business=screen.getByRole('button',{name:/Business/});expect(workers.style.getPropertyValue('--node-size')).not.toBe(business.style.getPropertyValue('--node-size'));expect(screen.getByText('LARGEST SOURCE')).toBeInTheDocument();expect(screen.getByText('LARGEST DESTINATION')).toBeInTheDocument();expect(screen.getByText('ONE NATIONAL POOL')).toBeInTheDocument()});
  it('hints at major composition before click',()=>{render(<App/>);expect(screen.getByLabelText('Income & social contributions territory')).toBeInTheDocument();expect(screen.getAllByLabelText('Income Tax').length).toBeGreaterThan(0);expect(screen.getByLabelText('Health territory')).toBeInTheDocument();expect(screen.getAllByLabelText('Medical services').length).toBeGreaterThan(0)});
  it('reveals official funding composition in the same world',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Income & social contributions/}));expect(screen.getByLabelText(/Income & social contributions official composition/)).toBeInTheDocument();expect(screen.getByText(/OFFICIAL RECEIPTS/)).toBeInTheDocument();expect(screen.getByText('Income Tax')).toBeInTheDocument();expect(screen.getByText('Social contributions')).toBeInTheDocument()});
  it('reveals a spending composition without leaking Health detail',async()=>{render(<App/>);await userEvent.click(screen.getByRole('button',{name:/Pensions/}));expect(screen.getByText(/THIS ALLOCATION CONTAINS/)).toBeInTheDocument();expect(screen.getByText('State pension')).toBeInTheDocument();expect(screen.queryByRole('button',{name:/Hospitals/})).not.toBeInTheDocument()});
  it('offers every hundred and billions as the only public units',()=>{render(<App/>);expect(screen.getByRole('button',{name:'EVERY £100'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'£ BILLIONS'})).toBeInTheDocument();expect(screen.queryByText('EVERY £1')).not.toBeInTheDocument()});
  it('shows technical reconciliation as neutral accounting rather than Other or leakage',()=>{render(<App/>);expect(screen.getByRole('button',{name:/Accounting £9.7/})).toHaveTextContent('RECONCILIATION · NOT A SERVICE');expect(screen.queryByRole('button',{name:/Other £15.6/})).not.toBeInTheDocument()});
  it('changes framing without changing visual geometry',async()=>{render(<App/>);const before=screen.getByRole('button',{name:/Income & social contributions £45/}).style.getPropertyValue('--node-size');await userEvent.click(screen.getByRole('button',{name:'£ BILLIONS'}));const after=screen.getByRole('button',{name:/Income & social contributions £610.652bn/}).style.getPropertyValue('--node-size');expect(after).toBe(before)});
});
