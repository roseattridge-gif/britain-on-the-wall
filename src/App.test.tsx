import {render,screen} from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import {describe,expect,it} from 'vitest'; import App from './App';
describe('Britain wall interactions',()=>{it('zooms into and out of Health without leaving the wall',async()=>{render(<App/>);await userEvent.click(screen.getByText('Health'));expect(screen.getByText('Inside the health system')).toBeInTheDocument();await userEvent.click(screen.getByText('Back to Britain'));expect(screen.getByText('Follow the public pound')).toBeInTheDocument()});it('opens evidence for a material number',async()=>{render(<App/>);const buttons=screen.getAllByLabelText(/View evidence for/);await userEvent.click(buttons[0]);expect(screen.getByText('Challenge the number')).toBeInTheDocument()});it('shows the contribution distinction when enabled',async()=>{render(<App/>);await userEvent.click(screen.getByText('Contribution & receipt'));expect(screen.getByText('Contribution ≠ moral worth')).toBeInTheDocument()})});
